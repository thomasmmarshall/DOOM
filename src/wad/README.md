# WAD Module

This module handles DOOM WAD file parsing and lump management:

- **WAD reader**: Parse WAD headers and directories
- **Lump manager**: Load and cache lumps by name
- **Data structures**: TypeScript interfaces for WAD data
